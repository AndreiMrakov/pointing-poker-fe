export interface IUser {
  id: string;
  role?: string;
  name: string;
  surname: string;
  job: string;
  avatar: string;
  isCardOpened: boolean;
  role?: string;
  score?: string;
  room: string;
}
